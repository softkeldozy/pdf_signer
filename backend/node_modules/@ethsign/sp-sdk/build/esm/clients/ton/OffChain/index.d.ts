import { OffChainClientBase } from '../../../interface/OffChainClientBase';
import { TonOffChainClientOptions } from '../types';
import { KeyPair } from '@ton/crypto';
import { TonConnectUI } from '@tonconnect/ui';
export declare class TonOffChainClient extends OffChainClientBase {
    walletClient: TonConnectUI;
    keyPair?: KeyPair;
    address?: string;
    private proofAnchor?;
    constructor(options: TonOffChainClientOptions);
    getAccount(): Promise<{
        address: string;
        publicKey: string | undefined;
    }>;
    signMessage(message: string): Promise<{
        signature: string;
        message: string;
        publicKey: string;
    }>;
    signTypedData(data: {
        message: {
            [key: string]: any;
        };
        types: {
            [key: string]: {
                name: string;
                type: string;
            }[];
        };
        primaryType: string;
    }): Promise<{
        message: any;
        signature: string;
        publicKey: string;
    }>;
}
//# sourceMappingURL=index.d.ts.map
import { OffChainSignType } from '../../types';
import { OffChainRpc } from '../../types/offChain';
import { KeyPair } from '@ton/crypto';
import { TonConnectUI } from '@tonconnect/ui';
export enum TonChains {
  mainnet = 'mainnet',
  testnet = 'testnet',
}

export type TonOffChainClientOptions = {
  signType: OffChainSignType.TonConnect;
  rpcUrl?: OffChainRpc | string;
  apiKey: string;
} & (
  | {
      walletClient: TonConnectUI;
    }
  | {
      account: KeyPair; //
      walletAddress: string; // if account is provided, walletAddress is required
    }
);
